spring.application.name=test-ops

# Database connection properties
spring.datasource.url=jdbc:postgresql://localhost:5432/ICTManager
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA properties
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

# Network Share Authentication (legacy - for machines without specific username)
network.share.username=ict_her
network.share.password=Tastatura135!

# VPN Authentication
network.share.vpnUsername=paradu2
network.share.vpnPassword=Atlanticaold1!

# Machine Access Authentication
machine.access.password=Tastatura135!

# VNC Configuration
vnc.viewer.path=D:\\Utile\\VNC\\vncviewer.exe

# Hibernate logging
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF

# Set Spring logging level
logging.level.org.springframework=WARN

server.shutdown=graceful

# Domain connection
server.address=0.0.0.0

# Server configuration for long-running operations
server.tomcat.connection-timeout=120000
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=10
server.tomcat.max-connections=8192
server.tomcat.accept-count=100

# Connection timeout settings
spring.mvc.async.request-timeout=120000

# Enable caching
spring.cache.type=simple

# JWT Configuration
app.jwtSecret=bG9ZGHashjGH7a8dkj68a9DasdsadPOa0sK3lasjhdL23SLKjdhaslkjdh732dsIUlaskjdhOPkjasdhUIY6
app.jwtExpirationMs=86400000